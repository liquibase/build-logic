name: Create Release

on:
  workflow_call:

jobs:
  sonar:
    uses: liquibase/build-logic/.github/workflows/sonar-push.yml@v0.2.3
    secrets: inherit
              
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Create Release Draft
        id: create-release
        uses: release-drafter/release-drafter@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
