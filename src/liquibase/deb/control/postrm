#!/bin/sh

# Remove the lines added by the postinst script from the user's shell configuration file
sed -i '/LIQUIBASE_HOME=\/opt\/liquibase/d' "$HOME/.bashrc"
sed -i '/export PATH=\$PATH:\$LIQUIBASE_HOME/d' "$HOME/.bashrc"

# Load the updated configuration into the current shell session
. "$HOME/.bashrc"